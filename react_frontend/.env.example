# React Frontend environment variables

# Required
REACT_APP_SUPABASE_URL=your_supabase_project_url
REACT_APP_SUPABASE_KEY=your_supabase_anon_key

# Optional: If set, app will use this for Supabase Auth emailRedirectTo and must match Supabase Auth Site URL/Redirect URLs
# Example: http://localhost:3000 or https://your-production-domain
REACT_APP_SITE_URL=http://localhost:3000

# Optional: Max PDF upload size (MB). Defaults to 50 if unset.
REACT_APP_MAX_UPLOAD_MB=50
